import { Container, Wrapper } from "layouts";
import styles from "./Orders.module.sass";

export const Orders = () => (
  <Wrapper>
    <Container>
      <div>Orders page</div>
    </Container>
  </Wrapper>
);
